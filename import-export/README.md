# Import, Export

## What I Learned
- 한 파일에서는 하나의 default export만 가능하고, named export는 여러 개가 존재할 수 있다. 
- 보편적으로 한 파일에서 하나의 컴포넌트만 export할 때 default export 방식을 사용하고 여러 컴포넌트를 export할 경우엔 named export 방식을 사용한다. 
- named import를 할 때에는 중괄호를 사용하여 가져온다. 